We first used a custom user model, but then realized that when we use that, Django doesnt hash the password for us, So we had to go back and then use Django's built in user model...So that we will be able to get that feature from Django
We first created the database in MySQL and then after that, we realized that we had forgot to add a field in our lab model, so we had to add a new one again, after adding the new field, we realized it affected the whole database cause we had already created some labs beforehand. So what we had to do was to give it a one-off default value or manually define a default value for that one in the models.py, and since we didnt want there to be stress annd possible future errors, we decided to provide a one-off default value for those ones that didnt have the newly added field. After providing the default value, what we then did was we went to replace those fields with the real values since they were serving as temporary placeholdeers using the django admin panel.. 
We are trying to get the forgot password send a link to the email to allow us to reset our password, but it seems like we have runn itoo a problem. The Wifi, and also the different email